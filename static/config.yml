backend:
  name: git-gateway
  branch: master

media_folder: static/img
public_folder: /img

collections:
  name: "blog"
  label: "Blog"
  folder: "content/blog"
  create: true
  slug: "{{path}}"
  editor:
    preview: false
  fields:
    - {label: "Title", name: "title", widget: "string"}